<UserControl x:Class="ProcSim.Wpf.Views.ProcessListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:conv="clr-namespace:ProcSim.Wpf.Converters"
             Height="Auto" Width="Auto">

    <UserControl.Resources>
        <conv:EnumDescriptionConverter x:Key="EnumDescriptionConverter"/>
    </UserControl.Resources>
    <!--<materialDesign:Card Background="{Binding CardBackground, RelativeSource={RelativeSource AncestorType=UserControl}}">-->
    <materialDesign:Card>
        <StackPanel Margin="5">
            <TextBlock Text="{Binding Header, RelativeSource={RelativeSource AncestorType=UserControl}, FallbackValue='My Header'}"
                   Style="{StaticResource MaterialDesignSubtitle1TextBlock}" />

            <Separator Margin="0,8"/>

            <DataGrid ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="False"
                  CanUserResizeColumns="False"
                  CanUserSortColumns="False"
                  HeadersVisibility="Column"
                  RowHeaderWidth="0"
                  GridLinesVisibility="None"
                  SelectionMode="Single"
                  BorderThickness="0"
                  Padding="0"
                  Focusable="False"
                  IsHitTestVisible="False">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="SizeToHeader" />
                    <DataGridTextColumn Header="Timer" Binding="{Binding RemainingTime}" Width="SizeToHeader" />
                    <DataGridTemplateColumn Header="Tipo" Width="SizeToHeader">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Type, Converter={StaticResource EnumDescriptionConverter}}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>

                <DataGrid.CellStyle>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Focusable" Value="False"/>
                    </Style>
                </DataGrid.CellStyle>
                <DataGrid.Resources>
                    <!-- Remove o fundo de seleção -->
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="Transparent"/>
                    <!-- Remove o contorno de célula selecionada -->
                    <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="Transparent"/>
                    <!-- Mantém o texto na cor preta mesmo quando selecionado -->
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="Black"/>
                </DataGrid.Resources>
            </DataGrid>
        </StackPanel>
    </materialDesign:Card>
</UserControl>
    