<UserControl x:Class="ProcSim.Wpf.Views.ProcessListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:conv="clr-namespace:ProcSim.Wpf.Converters" 
             Height="Auto" Width="Auto">

    <UserControl.Resources>
        <conv:EnumDescriptionConverter x:Key="EnumDescriptionConverter"/>
    </UserControl.Resources>

    <GroupBox Header="{Binding Header, RelativeSource={RelativeSource AncestorType=UserControl}}"
              FontSize="14" FontWeight="Bold" VerticalAlignment="Stretch" MinHeight="150">

        <DataGrid ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                  HeadersVisibility="Column" CanUserAddRows="False"
                  IsReadOnly="True" SelectionMode="Single" Focusable="False"
                  Background="Transparent" BorderThickness="0"
                  MinHeight="100" VerticalAlignment="Stretch" AutoGenerateColumns="False">

            <DataGrid.Columns>
                <DataGridTextColumn Header="Nome" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Tempo Restante" Binding="{Binding RemainingTime}" Width="*"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding Type, Converter={StaticResource EnumDescriptionConverter}}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </GroupBox>
</UserControl>
