<UserControl x:Class="ProcSim.Views.ProcessRegistrationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Height="Auto" Width="Auto">

    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="0,5,5,5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="0,5,0,5"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Width" Value="150"/>
        </Style>

        <Style TargetType="StackPanel">
            <Setter Property="Margin" Value="0,5,0,5"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <!-- Largura fixa para rótulos -->
            <ColumnDefinition Width="*"/>
            <!-- Campos ocupam o restante -->
        </Grid.ColumnDefinitions>

        <!-- Nome do Processo -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Nome:"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name}"/>

        <!-- Tempo de Execução -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Tempo Execução:"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding ExecutionTime}"/>

        <!-- Tipo do Processo -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Tipo:"/>
        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Left">
            <RadioButton Content="CPU-bound" IsChecked="true" GroupName="ProcessType"/>
            <Separator Width="10" Visibility="Hidden"/>
            <RadioButton Content="I/O-bound" IsChecked="{Binding IsIoBound}" GroupName="ProcessType"/>
        </StackPanel>

        <!-- Tempo de I/O (Somente visível se IsIoBound == true) -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="Tempo I/O:" Visibility="{Binding IsIoBound, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        <TextBox Grid.Row="3" Grid.Column="1" Name="tbIoTime" Text="{Binding IoTime}" Visibility="{Binding IsIoBound, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <!-- Botão Adicionar Processo -->
        <Button Grid.Row="5" Grid.ColumnSpan="2" Content="Adicionar" Command="{Binding AddProcessCommand}" HorizontalAlignment="Center" Width="100" />
    </Grid>
</UserControl>
