using ProcSim.Core;
using ProcSim.Core.Enums;
using ProcSim.Core.Models;
using ProcSim.Core.Scheduling;

namespace ProcSim.Tests;

public class SchedulerTests
{
    //[Fact]
    //public async Task FCFS_Should_CompleteAllProcessesAsync()
    //{
        // Arrange
        //CancellationToken ct = new();
        //Queue<Process> processes = new(Enumerable.Range(1, 3).Select(i => new Process(i, $"P{i}", i * 1, 0, ProcessType.CpuBound)));
        //Scheduler scheduler = new();
        //ISchedulingAlgorithm algorithm = new FcfsScheduling();

        //// Act
        //await scheduler.RunAsync(processes, algorithm, ct);

        // Assert
        // If the code reaches here, all processes should be completed
        // You might add more specific assertions if needed.
    //}
}
